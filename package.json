{
  "name": "lingshu-palm-ai",
  "version": "2.0.0",
  "description": "灵枢 — AI掌纹溯源 PWA/App",
  "main": "static/index.html",
  "scripts": {
    "cap:init": "npx cap init 灵枢 com.lingshu.palmai --web-dir=static",
    "cap:android": "npx cap add android",
    "cap:ios": "npx cap add ios",
    "cap:sync": "npx cap sync",
    "cap:open:android": "npx cap open android",
    "cap:open:ios": "npx cap open ios",
    "cap:build:android": "npx cap sync && cd android && ./gradlew assembleDebug",
    "cap:build:ios": "npx cap sync && npx cap open ios"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "@capacitor/android": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  },
  "author": "",
  "license": "UNLICENSED",
  "private": true
}
